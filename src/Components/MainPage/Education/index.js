import React from 'react';
import './index.scss';
import {Outlet} from "react-router-dom";

const Education = () => {
    return (
        <>
            <Outlet/>
        </>
    );
};

export default Education;
