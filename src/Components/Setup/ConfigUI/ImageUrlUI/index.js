import './index.scss'
import React from 'react';

const ImageUrlUI = ({onChange, isGhPage, info, name}) => {
    return (
        <>
            imageUrlUI
        </>
    );
};

export default ImageUrlUI;
