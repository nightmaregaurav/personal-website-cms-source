import React from 'react';
import './index.scss'

const UrlUI = ({onChange, info, name, parent_disabledStatus}) => {
    return (
        <>
          urlUI
        </>
    );
};

export default UrlUI;
